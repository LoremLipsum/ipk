mixin heading-lvl-2(data)
    +el({
        tag: data.tag,
        class: 'heading-lvl-2',
        mix: data.mix,
        mod: data.mod,
        js: data.js,
        text: data.text,
    })&attributes(attributes)
